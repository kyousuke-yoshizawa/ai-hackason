// 架空エリア「ことこと町」のランドマーク定義（要件定義書v2 2.2節）。
// 変更頻度が低く、店舗のように管理画面から編集する想定もないため、
// DBテーブルを新設せずコード定数として保持する。
//
// 座標変換について：要件定義書は原点(0,0)中心の座標系（負値を含む）だが、
// 実装（MapPicker/StoreForm、stores.x/y）は 0〜400 の正値のみを扱うため、
// オフセット (+200, +200) を適用した変換後座標をここに定義する
// （Issue #101 / 要件定義書v2 2.2.1節の変換表を参照）。
export interface Landmark {
  name: string
  x: number
  y: number
  kind: string
}

export const AREA_NAME = 'ことこと町'

export const LANDMARKS: Landmark[] = [
  // どんぐり広場（町の中心・待ち合わせスポット）。要件座標(0,0) → 変換後(200,200)
  { name: 'どんぐり広場', x: 200, y: 200, kind: 'plaza' },
  // ぽかぽか駅（中央駅）。要件座標(0,-180) → 変換後(200,20)
  { name: 'ぽかぽか駅', x: 200, y: 20, kind: 'station' },
  // しっぽ公園（子連れ向け）。要件座標(150,120) → 変換後(350,320)
  { name: 'しっぽ公園', x: 350, y: 320, kind: 'park' },
  // ぷかぷか商店街（東西のメイン通り、y≈30 → 変換後y=230）。
  // 要件座標は範囲表現(-150〜150, 30)のため、通りの両端2点として保持する
  // （地図描画側で2点を結んで通りを表現できるようにするため）
  { name: 'ぷかぷか商店街（西端）', x: 50, y: 230, kind: 'shopping_street' },
  { name: 'ぷかぷか商店街（東端）', x: 350, y: 230, kind: 'shopping_street' },
]
