# 要件ギャップ＆リファクタリング監査報告（2026-07-15）

要件定義書 `docs/requirements/outing-plan-ai-requirements-v2.md` と実装の突き合わせ監査、
およびコードベース全体の共通化・冗長性調査の結果。
**検出した全項目は GitHub Issue #96〜#110 として登録済み**（各Issueに下位モデルが即実装できる粒度の手順を記載）。

## 1. 総括

- プラン生成AIの**バックエンドは要件に忠実に実装済み**（`backend/domains/plan/` + `POST /api/plan/generate`。
  Claude 1回呼び出し統合・スコアリング重み・距離感タグ閾値すべて要件どおり）
- 一方で**ユーザー向けのプラン体験（チャットUI・複数案表示・SVGマップ）が丸ごと未実装**。
  `src/lib/plan.ts` の `generatePlan()` はどの画面からも呼ばれていない未接続状態
- 店舗側機能はS001（店舗登録）とS006（座標配置UI）が完了。S004（オファー）は完全未実装で、
  スコアリングのオファー15%枠が常時0の不活性状態
- 要件の「ことこと町」8店舗シード・ランドマーク座標・`/api/area` は未投入・未実装
- 既知課題（refactoring-handbook T01〜T23）のうち Critical/High はほぼ解消済み。
  残る構造課題は T14（フロント共通化）・T15（型一元化）で、今回のIssueで具体化した

## 2. 要件カバレッジ判定

| 要件ID | 機能 | 判定 | 対応Issue |
|---|---|---|---|
| U001 | チャット入力UI | ❌ 未実装 | #96 |
| U002 | 意図解析 | ✅ 実装済み（BE） | —（起動UIは #96） |
| U003 | 店舗検索 | ✅ 実装済み | — |
| U004 | プラン生成表示（複数案） | 🔶 BEのみ・UI無し | #96 |
| U005 | プラン詳細表示 | 🔶 評価/営業時間/待ち時間/オファーがstop出力に無い | #97 |
| U006 | 会話履歴（セッション内） | ❌ 未実装 | #103 |
| U007 | 簡易SVGマップ表示 | ❌ 未実装（MapPickerは入力用） | #102 |
| S001 | 店舗登録フォーム | ✅ 実装済み | — |
| S002 | 混雑パターン設定（曜日×時間帯） | 🔶 曜日次元・グリッドUI無し | #99 |
| S003 | リアルタイム混雑更新ボタン | 🔶 BEのみ・ボタンUI無し | #100 |
| S004 | オファー設定 | ❌ 完全未実装（テーブル/API/UI/スコア結線） | #98 |
| S005 | 店舗ダッシュボード | 🔶 本日の混雑状態・オファー表示無し | #100 |
| S006 | 座標配置UI | ✅ 実装済み（MapPicker） | — |
| 2.2節 | ことこと町8店舗シード・ランドマーク | ❌ 別データで代替中 | #101 |
| 6.2節 | `/api/area` | ❌ 未実装 | #101 |
| 5章 | スコアリング | 🔶 オファー15%が不活性 | #98 |

補足:
- 技術スタックは要件（Next.js + SQLite）と実装（Vite SPA + Supabase + Vercel Functions）が乖離しているが、
  チーム決定済みの構成であり是正対象とはしない（要件書側の更新は任意）
- 座標系は要件が負座標（原点中心）、実装が0〜300の正値のみ。#101 でオフセット(+200,+200)＋グリッド400への拡張で吸収する

## 3. リファクタリング計画

実害・ROIの高い順。詳細手順は各Issue参照。

| # | 内容 | 規模・実害 | Issue |
|---|---|---|---|
| 1 | 店舗一覧のいいね数二重取得削除＋stores集計のN+1解消 | サーバ3N＋フロントNクエリ → 3〜4に | #105 |
| 2 | バックエンドHTTPハンドラの定型句共通化（try/catch 14箇所・405 6箇所・param検証5箇所・cron 2本がほぼ同一・x-user-id抽出） | 約100行削減 | #104 |
| 3 | `src/lib` の ApiResult 変換 try/catch（4ファイル13箇所）→ `toApiResult` | 約70行削減 | #106 |
| 4 | `useApiQuery`（9ファイルの取得定型）＋PageHeader（7ページ）＋Loading/Error/Empty＋Modal統一 | 約250行削減、T14の一部 | #107 |
| 5 | 管理パネル2種の約80%重複 → `useMasterTable`・`useZodForm`・`CategorySelect` | 最大の重複、150行超 | #108 |
| 6 | `shared/types` 新設（Reservation/Like/Review/StoreMedia/CongestionLevel の二重定義解消）＋混雑度ラベル4定義の文言統一 | T15実施、UX不整合の実害あり | #109 |
| 7 | Supabase定型句（`unwrap`）・部分更新構築・カラム定数の集約 | 数十箇所の1行化 | #110 |

### 実施順の推奨

1. **#105**（即効・低リスク・実害解消）
2. **#104 / #106 / #110**（純粋な共通化。互いに独立、並行可）
3. **#107 → #108**（useApiQuery を先に入れ、useMasterTable がそれを利用する構成）
4. **#109**（横断的なため、他のリファクタが落ち着いたタイミングで）

⚠️ #107/#108 は `StoresPage.tsx` を触るため、進行中の #85（店舗検索UI）との競合に注意。

## 4. 既知課題（T01〜T23）の現況

コードから確認した実装状況（2026-07-15時点）:

- ✅ 完了: T01, T03〜T13, T18（Critical/Highはほぼ解消）
- ❌ 未完了: **T02**（CIパイプライン。`.github/workflows/` は sync-notion.yml のみ）、
  **T14**（→ #107/#108 で具体化）、**T15**（→ #109 で具体化）、
  **T16**（jest projects分割）、**T17**（マイグレーション番号正規化）、T22（ブランチ保護・人間作業）
- T02/T16/T17 は今回Issue化していない（refactoring-handbook に手順が既載のため。必要ならIssue化を追加検討）

## 5. 機能Issueの依存関係

```
#101 ことこと町マスタ ──┐
#96 プラン生成画面 ──┬──→ #102 SVGマップ（MapView）
                     ├──→ #103 会話履歴・修正リクエスト
#97 プラン詳細拡充 ──┘（表示部のみ#96後）
#98 オファー機能 ────→ #100 の「オファー表示」追加分
#99 混雑パターングリッド（独立）
#100 ダッシュボード強化（独立・BE実装済み）
```

デモのクリティカルパス: **#101 → #96 → #102**（世界観データ → プラン画面 → マップ可視化）
