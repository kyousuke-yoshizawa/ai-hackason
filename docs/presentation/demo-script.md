# デモ台本（7/25・約10分プレゼン内のデモパート）

関連Issue: #153

> ⚠️ **現状のステータス（2026-07-16時点）**: 本台本は骨子（構成・進行案）のみを先行作成したものです。シーン1・シーン3が前提とする「プラン生成画面（チャット入力＋複数案表示）」は #96 が、シーン2が前提とする「店舗ダッシュボードのリアルタイム混雑ボタン」は #100 が、それぞれ**未実装（OPEN）**のため、入力文言の確定・店舗操作でプランが変わることの実地検証・リハーサルはまだ実施できていません。`presentation.md` 側も「AIプラン提案 🚧: API実装完了、UI画面は次フェーズでデモに追加予定」と記載の通りです。#96・#100 実装後、本台本の該当セクションを具体的な文言・実測値で埋め、リハーサルを実施してください（受け入れ条件の該当項目は未達のまま残しています）。

## 前提チェックリスト

デモ開始前に以下を確認する。

- [ ] シード適用済み（#101 ことこと町8店舗マスタ、#151 レビューシード）
- [ ] Vercel本番のヘルスチェック（現状ヘルスチェック専用エンドポイントは未実装。`/api/area` 等、軽量なGETエンドポイントへの疎通確認で代替する）
- [ ] Claude APIのモック切り替え変数（現状 `PLAN_MOCK` 相当の仕組みは未実装。障害時代替手段は「予備シナリオ」参照）
- [ ] デモ用アカウントでログイン済みタブを用意（`yoshizawa@ai-hackason.example` 等、テストアカウント4名のいずれか）
- [ ] 混雑・オファーの初期状態を、後の操作で変化が見えるように事前設定しておく（例: 「ことりの休憩処」を混雑状態にしておき、シーン2でリアルタイム更新ボタンにより変化させる。※リアルタイム更新ボタンは#100実装後に確定）

## シーン1（目安2分）: ユーザー側 — プラン生成

> #96（プラン生成画面）実装後に文言・操作手順を確定する。

| 話すこと | 操作すること | 見せる画面 |
|---|---|---|
| 「ことこと町にお出かけしたいユーザーが、AIにチャットで要望を伝えると…」 | （確定次第記載: 事前に何度も試して当たりが出る固定入力文をここに一字一句記載する） | プラン生成画面（チャット入力欄） |
| 「複数の案が提示され、それぞれスコア・意図バッジ・マップ上の位置が確認できます」 | 生成結果の複数案を表示 | 複数案表示画面（意図バッジ・スコア・マップ） |
| （意図バッジ→スコア→マップの順に指差しで解説） | カーソルで各要素を指す | 同上 |

## シーン2（目安2分）: 店舗側 — リアルタイム混雑・オファー設定

> #100（店舗ダッシュボードのリアルタイム混雑ボタン）実装後に文言・操作手順を確定する。

| 話すこと | 操作すること | 見せる画面 |
|---|---|---|
| 「一方、店舗側は今の混み具合をワンタップで発信できます」 | 「ことりの休憩処」の管理画面で「空いてる」ボタンを押す（#100実装後にボタン名を確定） | 店舗管理ダッシュボード |
| 「オファーを設定すれば、AIのプラン提案でも優先的に案内されます」 | オファー設定（例: 「14-16時は狙い目！20%OFF」）を入力 | オファー設定フォーム |
| 「店舗側は特別な操作なしで、リアルタイムに集客につなげられます」 | （1文でまとめる） | — |

## シーン3（目安2分）: ユーザー側に戻り再生成 — 変化の実演

> シーン2の操作がシーン1の結果に反映されることを**事前に検証**した上で最終化する。変わらない場合は代替店舗（例: 「きらきらアイス堂」等）をあらかじめ用意しておく。

| 話すこと | 操作すること | 見せる画面 |
|---|---|---|
| 「先ほど店舗側で行った操作が、AIのプランに即座に反映されます」 | シーン1と同じ入力でプランを再生成 | プラン生成画面 |
| 「『ことりの休憩処』のスコア・優先度が変わっているのがわかります」 | 変化した箇所（スコア・混雑表示・オファーバッジ）を指す | 複数案表示画面 |

## 予備シナリオ

### Claude API障害時

- 現状、モック切り替え（要件書7章で言及された `PLAN_MOCK` 相当）の仕組みは未実装。障害発生時は「本日はAPIが混み合っているようです」と話し、事前に用意したスクリーンショット（下記）で説明を継続する
- モック切り替えの仕組み自体を用意するかは別途検討（現状は未着手）

### Wi-Fi死亡時

- スクリーンショット集: **未作成**。#96のUI実装後、シーン1〜3の各画面のスクリーンショットを撮影し、`docs/presentation/screenshots/`（未作成）に格納する想定
- スクリーンショットのみでも一連の流れ（要望入力→複数案→店舗操作→再生成）を説明できるよう、キャプション付きで並べる

## リハーサル結果

- **未実施**。#96・#100の実装完了後にリハーサルを行い、実測時間と詰まった点をここに追記する（issue #153へのコメントも行う）

## 関連ドキュメント

- 発表資料: `docs/presentation/presentation.md`（「デモで説明する内容」セクションから本ファイルへリンク）
- 要件定義書: `docs/requirements/outing-plan-ai-requirements-v2.md`（10章 成功基準）
